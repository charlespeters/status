<strong>{{ model.name }}</strong> (total {{ model.publicRepos }})

<ul>
  {{#each model.publicRepos as |repo|}}
    <li>{{repo.fullName}} ({{repo.language}}, {{repo.stargazersCount}} stars)</li>
  {{/each}}
</ul>


{{outlet}}
